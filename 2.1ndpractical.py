list_of_names = ["sonam", 22 , "chhimi", "pelden"]
# the list of names are numbered in ascending order starting from zero so in the previous on sonam is 0 and pelden is 2. 
print (list_of_names[3])

#ADDINGS TO AN ARRAY
new_list = "Pem"
list_of_names.append(new_list) # append lest us add thiings to the array.
print (list_of_names[4]) 
 
fav_colour = ("gray, ")
print(len(fav_colour)) # counts letter - len
# 
print (fav_colour.find('y')) # lets us know the location of the letter
print (fav_colour.count('y')) # counts the number of letters.
